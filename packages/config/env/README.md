# Environment Templates

Runtime environment examples live next to the apps that consume them:

- `apps/web/.env.example`
- `apps/api/.env.example`
- `apps/worker/.env.example`

Shared config conventions should be documented here as they become stable. Do not place real secrets in this package.

