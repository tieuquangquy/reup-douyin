import { pageMetadata } from "../../../lib/pageMetadata";
import { CaptureInboxPage } from "../../../components/capture-inbox/CaptureInboxPage";

export const metadata = pageMetadata.captureInbox;

export default function Page() {
  return <CaptureInboxPage />;
}
