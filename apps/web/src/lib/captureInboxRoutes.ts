/** Canonical Operator Studio route for Capture Inbox (Collect → Review → Queue). */
export const CAPTURE_INBOX_HREF = "/selection/capture-inbox";

/** Pre-Studio Ops Console path — keep redirecting for bookmarks and old extension builds. */
export const CAPTURE_INBOX_LEGACY_HREF = "/ops/extensions/douyin/capture-inbox";
