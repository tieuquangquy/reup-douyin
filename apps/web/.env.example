NEXT_PUBLIC_APP_NAME=reup-douyin
NEXT_PUBLIC_API_BASE_URL=/api
# Legacy Douyin debug surfaces are hidden by default. Enable only for explicit
# debugging/migration sessions; the main operator UI is browser-profile-backed.
NEXT_PUBLIC_DOUYIN_ENABLE_LEGACY_DEBUG_SURFACES=false
